name: kustomize-diff

on:
  pull_request:

jobs:
  kustomize-diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: AdrienneCohea/kustomize-diff@v1.0.2
        with:
          search-path: .xyz
