name: kustomize-diff

on:
  push:
    branches-ignore:
      - main

jobs:
  kustomize-diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: AdrienneCohea/kustomize-diff@v1
        with:
          search-path: .xyz
